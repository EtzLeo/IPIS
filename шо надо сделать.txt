мне не хватает:

    GenderController:
        GET_GENDER_LIST -- возврат списка гендеров
        SET_CURRENT_GENDER -- создает новый гендер из строки и ставит его текущим
        IS_NEW_GENDER -- вляется ли новым
        SAVE_CURRENT -- добавляет текущий гендер в список

    UserModel:
        RESEST_WITH -- замена всех полей текущего юзера кроме id
        GET_USER_DATA -- все поля в итерируемый объект

    UserController:
        FIND_CURRENT_USER -- принимает серию номер паспорта и дату заселения, ищет
    		                 совпадения в списке и найденный ставит как current_user

    +
        В конструкторы и того и другого я передаю все касающееся их содержимое базы списком
        Прикрути преобразование до объектов
        Скорее всего при этом надо будет наладить между ними взаимодействие
        Потому что гендер в ЮзерМодел -- объект класса Гендер (которого у нас нет)
        Поля current... и isNew -- публичные! Иначе у меня нет к ним доступа
